// --- AI モデルの既定値 ---
// モデル名が変わっても設定画面から差し替えられるよう、既定値をここに集約する。

/** テキストから画像を生成するモデル（Imagen） */
export const DEFAULT_TEXT_TO_IMAGE_MODEL = 'imagen-4.0-generate-001';

/** 参照画像をもとに画像を生成するモデル（Gemini image） */
export const DEFAULT_IMAGE_TO_IMAGE_MODEL = 'gemini-2.5-flash-image';
